import ReBreaderumbItem from '../remo-breadcrumb/src/remo-breadcrumb-item.vue'

ReBreaderumbItem.install = Vue => {
  Vue.component(ReBreaderumbItem.name, ReBreaderumbItem)
}

export default ReBreaderumbItem
